{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to MITTA's two windows. Deliberately narrow: every entry here is an API the webview can reach, and the webview renders model output.",
  "windows": ["main", "palette"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:event:default",
    "global-shortcut:default"
  ]
}
